-- 🐲 HORSE RACE 250% PET POCKET 🐲
-- DELTA EXECUTOR

-- GUI STRUCTURE
local ScreenGui = Instance.new("ScreenGui")
local Main = Instance.new("Frame")
local Title = Instance.new("TextLabel")

ScreenGui.Parent = game.CoreGui

Main.Parent = ScreenGui
Main.Size = UDim2.new(0,340,0,420)
Main.Position = UDim2.new(0.03,0,0.18,0)
Main.BackgroundColor3 = Color3.fromRGB(25,25,25)
Main.Active = true
Main.Draggable = true

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0,16)

Title.Parent = Main
Title.Size = UDim2.new(1,0,0,50)
Title.BackgroundColor3 = Color3.fromRGB(255,0,0)
Title.Text = "🐲 250% PET HUB 🐲"
Title.TextScaled = true
Title.Font = Enum.Font.GothamBlack
Title.TextColor3 = Color3.new(1,1,1)
Title.Active = false
Title.Draggable = false

local TC = Instance.new("UICorner", Title)

-- PET VISUAL
local Pet = Instance.new("TextLabel")
Pet.Parent = Main
Pet.Position = UDim2.new(0.18,0,0.16,0)
Pet.Size = UDim2.new(0,220,0,90)
Pet.BackgroundColor3 = Color3.fromRGB(45,45,45)
Pet.Text = "🔥 PET EXCLUSIVO 250% 🔥"
Pet.TextScaled = true
Pet.Font = Enum.Font.GothamBold
Pet.TextColor3 = Color3.new(1,0,0)
Pet.Active = false
Pet.Draggable = false

local PC = Instance.new("UICorner", Pet)

-- BUTTON CREATION FUNCTION
local function CreateButton(text,pos,color)
	local Button = Instance.new("TextButton")

	Button.Parent = Main
	Button.Size = UDim2.new(0,250,0,40)
	Button.Position = pos
	Button.BackgroundColor3 = color
	Button.Text = text
	Button.TextScaled = true
	Button.Font = Enum.Font.GothamBold
	Button.TextColor3 = Color3.new(1,1,1)
	Button.Active = true
	Button.Draggable = false

	local Corner = Instance.new("UICorner", Button)
	Corner.CornerRadius = UDim.new(0,8)

	return Button
end

-- CREATE BUTTONS
local Equip = CreateButton(
	"🐲 EQUIP 250% PET",
	UDim2.new(0.13,0,0.42,0),
	Color3.fromRGB(170,0,255)
)

local Hatch = CreateButton(
	"🥚 AUTO HATCH",
	UDim2.new(0.13,0,0.56,0),
	Color3.fromRGB(255,170,0)
)

local Reward = CreateButton(
	"🎁 AUTO REWARD",
	UDim2.new(0.13,0,0.70,0),
	Color3.fromRGB(0,170,255)
)

local Close = CreateButton(
	"❌ CLOSE",
	UDim2.new(0.13,0,0.84,0),
	Color3.fromRGB(255,0,0)
)

-- BUTTON FUNCTIONS
Equip.MouseButton1Click:Connect(function()
	game.StarterGui:SetCore("SendNotification",{
		Title = "250% PET",
		Text = "Pet equipado!",
		Duration = 3,
		Icon = "rbxasset://textures/Icon.png"
	})
end)

Hatch.MouseButton1Click:Connect(function()
	game.StarterGui:SetCore("SendNotification",{
		Title = "AUTO HATCH",
		Text = "Abrindo ovos...",
		Duration = 3,
		Icon = "rbxasset://textures/EggIcon.png"
	})
end)

Reward.MouseButton1Click:Connect(function()
	game.StarterGui:SetCore("SendNotification",{
		Title = "AUTO REWARD",
		Text = "Coletando recompensas...",
		Duration = 3,
		Icon = "rbxasset://textures/RewardIcon.png"
	})
end)

Close.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- ADDITIONAL FEATURES
local Settings = CreateButton(
	"⚙️ SETTINGS",
	UDim2.new(0.13,0,0.28,0),
	Color3.fromRGB(100,100,100)
)

Settings.MouseButton1Click:Connect(function()
	local settingsMenu = Instance.new("Frame")
	settingsMenu.Name = "SettingsMenu"
	settingsMenu.Size = UDim2.new(0,300,0,200)
	settingsMenu.Position = UDim2.new(0.5,-150,0.5,-100)
	settingsMenu.BackgroundColor3 = Color3.fromRGB(30,30,30)
	settingsMenu.Visible = true

	local settingsLabel = Instance.new("TextLabel", settingsMenu)
	settingsLabel.Text = "CONFIGURAÇÕES"
	settingsLabel.TextScaled = true
	settingsLabel.Font = Enum.Font.GothamBold
	settingsLabel.TextColor3 = Color3.new(1,1,1)
	settingsLabel.Size = UDim2.new(0,300,0,40)
	settingsLabel.Position = UDim2.new(0,0,0,0)
	settingsLabel.BackgroundColor3 = Color3.fromRGB(255,0,0)

	local closeButton = Instance.new("TextButton", settingsMenu)
	closeButton.Text = "X"
	closeButton.TextSize = 24
	closeButton.Size = UDim2.new(0,30,0,30)
	closeButton.Position = UDim2.new(0,270,0,5)
	closeButton.BackgroundColor3 = Color3.fromRGB(255,0,0)
	closeButton.TextColor3 = Color3.new(1,1,1)

	closeButton.MouseButton1Click:Connect(function()
		settingsMenu:Destroy()
	end)
end)
